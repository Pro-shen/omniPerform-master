<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.omniperform.web.mapper.KnowledgeCategoryMapper">
    
    <resultMap type="KnowledgeCategory" id="KnowledgeCategoryResult">
        <result property="categoryId"    column="category_id"    />
        <result property="categoryCode"    column="category_code"    />
        <result property="categoryName"    column="category_name"    />
        <result property="description"    column="description"    />
        <result property="parentId"    column="parent_id"    />
        <result property="sortOrder"    column="sort_order"    />
        <result property="status"    column="status"    />
        <result property="delFlag"    column="del_flag"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectKnowledgeCategoryVo">
        select category_id, category_code, category_name, description, parent_id, sort_order, status, del_flag, create_by, create_time, update_by, update_time from knowledge_category
    </sql>

    <select id="selectKnowledgeCategoryList" parameterType="KnowledgeCategory" resultMap="KnowledgeCategoryResult">
        <include refid="selectKnowledgeCategoryVo"/>
        <where>  
            <if test="categoryCode != null  and categoryCode != ''"> and category_code = #{categoryCode}</if>
            <if test="categoryName != null  and categoryName != ''"> and category_name like concat('%', #{categoryName}, '%')</if>
            <if test="status != null  and status != ''"> and status = #{status}</if>
        </where>
        order by sort_order asc, create_time desc
    </select>
    
    <select id="selectKnowledgeCategoryByCategoryId" parameterType="Long" resultMap="KnowledgeCategoryResult">
        <include refid="selectKnowledgeCategoryVo"/>
        where category_id = #{categoryId}
    </select>

    <select id="selectKnowledgeCategoryByCategoryCode" parameterType="String" resultMap="KnowledgeCategoryResult">
        <include refid="selectKnowledgeCategoryVo"/>
        where category_code = #{categoryCode}
    </select>

    <select id="selectActiveCategories" resultMap="KnowledgeCategoryResult">
        <include refid="selectKnowledgeCategoryVo"/>
        where status = '0' and del_flag = '0'
        order by sort_order asc, create_time desc
    </select>

    <select id="countKnowledgeByCategoryId" parameterType="Long" resultType="int">
        select count(*) from knowledge_base where category_id = #{categoryId} and status = '0'
    </select>
        
    <insert id="insertKnowledgeCategory" parameterType="KnowledgeCategory" useGeneratedKeys="true" keyProperty="categoryId">
        insert into knowledge_category
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="categoryCode != null and categoryCode != ''">category_code,</if>
            <if test="categoryName != null and categoryName != ''">category_name,</if>
            <if test="description != null">description,</if>
            <if test="parentId != null">parent_id,</if>
            <if test="sortOrder != null">sort_order,</if>
            <if test="status != null">status,</if>
            <if test="createBy != null">create_by,</if>
            create_time
         </trim>
         <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="categoryCode != null and categoryCode != ''">#{categoryCode},</if>
            <if test="categoryName != null and categoryName != ''">#{categoryName},</if>
            <if test="description != null">#{description},</if>
            <if test="parentId != null">#{parentId},</if>
            <if test="sortOrder != null">#{sortOrder},</if>
            <if test="status != null">#{status},</if>
            <if test="createBy != null">#{createBy},</if>
            sysdate()
         </trim>
    </insert>

    <update id="updateKnowledgeCategory" parameterType="KnowledgeCategory">
        update knowledge_category
        <trim prefix="SET" suffixOverrides=",">
            <if test="categoryCode != null and categoryCode != ''">category_code = #{categoryCode},</if>
            <if test="categoryName != null and categoryName != ''">category_name = #{categoryName},</if>
            <if test="description != null">description = #{description},</if>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            update_time = sysdate()
        </trim>
        where category_id = #{categoryId}
    </update>

    <delete id="deleteKnowledgeCategoryByCategoryId" parameterType="Long">
        delete from knowledge_category where category_id = #{categoryId}
    </delete>

    <delete id="deleteKnowledgeCategoryByCategoryIds" parameterType="String">
        delete from knowledge_category where category_id in 
        <foreach item="categoryId" collection="array" open="(" separator="," close=")">
            #{categoryId}
        </foreach>
    </delete>

</mapper>